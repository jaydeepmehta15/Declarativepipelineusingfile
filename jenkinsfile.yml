pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                //sh 'npm --version'
                bat 'echo "Hello World from Jenkins File"'
            }
        }
    }
}
